require 'spec_helper'

describe BiblioPrueba do
  it 'has a version number' do
    expect(BiblioPrueba::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
